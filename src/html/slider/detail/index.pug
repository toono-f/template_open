extends ../../_layout/_page.pug

block vars
  -
    var page = data.pages.slider.detail.index
    //- 以下 title属性に祖先要素や親要素を追加したい場合、変数parentsに["祖先要素", "親要素"]の順に値を代入する。
    //- var parents = [data.pages.slider.index]

block contentFixedBefore

block contentStatic

  +heading([data.pages.slider.index, page])

block contentFixedAfter